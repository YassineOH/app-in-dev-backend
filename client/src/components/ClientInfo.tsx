import { FC } from "react";

const ClientInfo: FC = () => {
  return (
    <div className="h-screen w-screen bg-opacity-50 bg-slate-800 fixed top-0 left-0 right-0 flex items-center justify-center"></div>
  );
};
export default ClientInfo;
